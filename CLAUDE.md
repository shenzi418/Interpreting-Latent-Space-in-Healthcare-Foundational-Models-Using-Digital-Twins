# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Thesis**: *Interpreting the Latent Space in ECG Digital-Twin Foundation Models* — a 1-year MRes project, in write-up. **Thesis due 2026-08-28** (⚠ moved from 08-31 — programme-director email 2026-08-17). **Viva Sept 7–18**, 45 min: 20-min student presentation + Q&A from supervisor AND second marker (same second marker as the poster; book the slot with both EARLY). Mandatory Declarations chapter (ethics, data availability, sustainability, and a required Use-of-Generative-AI disclosure — AI-produced falsehoods fall under academic-misconduct policy, which makes the number-tracing discipline an integrity requirement). Paper submission is a stretch goal if results are strong enough.

**Active state of the research:**

- **⚠ 2026-08-13 — EXPERIMENTAL FREEZE. Research phase closed; thesis writing begins.** Every number the thesis needs exists, is adversarially verified, and lives in two reports: `reports/2026-08-13_fidelity_audit_and_final_verification.md` (complete corrected record: Tier-1 geometry retractions, floor methodology, fidelity audit) and `reports/2026-08-13_thesis_endgame_decision.txt` (headline claim, six-claim ladder, **26-item forbidden-phrasings list — grep the draft against it**, day-by-day writing plan M1–M9, ML4H Findings Sep 10 verdict, supervisor questions Q1–Q5). Headlines: (1) **CLAIM 2 / S(b) / b̂ is RETRACTED** — S(b) is branch-invariant only at integer b (Kempter 2012 prior art; banner in `analysis/cyclic_order_test.py`); (2) **circular R has a constant-predictor floor (0.29216 PTB-XL / 0.09319 MedalCare)** that every Tier-1 transport number sits below — never quote R without it, and permutation nulls sit *below* the floor so they cannot substitute (`analysis/floor_audit.py`); (3) **the fidelity audit is the thesis centrepiece**: MedalCare-XL inverts the measure-block ranking of territory information (block-mean ρ=−0.587; per-feature ρ=−0.354 is NOT feature-level significant, block-aware p=0.10–0.14; 28+axis blind spots, 9/12 ST leads spurious, all CI-backed; axis η² 0.0020 sim vs 0.1297 real at matched dispersion); the audit **predicts raw transfer** (ST at the constant floor, efficiency 0.006; η²_real↔efficiency ρ=+1.00; exact one-sided p=0.0417, fold-seed-fragile — the robust facts are the paired block orderings) and **repair fails** (channel restriction −0.055 p<0.001; reweighting *harms*; best restriction = 12-dim inferior-leads parity, reported as parity); (4) **the scaler dichotomy sharpened Q1**: the zero-shot frame positive exists under the target scaler only (random-projection + shuffled-refit nulls p=0.0033; source η² p=0.34), F2's prediction under source only, and the supervised increment over the axis fails its null (p=0.093) under both — Q1 is the gating supervisor call. In-domain mirror unifies everything: latent 0.6195 ≫ control54 0.4535 ≫ axis 0.2050 (below floor) in-domain; all three collapse to 0.34/0.32/0.30 cross-domain (latent−control +0.1660 replicates the established +0.1523). Verified scripts: `analysis/fidelity_audit.py`, `analysis/block_transfer.py`, `analysis/channel_repair.py`; artifacts: `reports/2026-08-13_audit_artifacts/`.
- **⚠ 2026-08-10 audit — every pre-`exp8` number is provisional.** A repo-wide audit found four confirmed defects that touch the training data itself: (1) MedalCare batches had aVL/aVF transposed while PTB-XL did not; (2) MedalCare used a global-scalar z-score while PTB-XL used per-lead; (3) the PTB-XL 3-class filter kept STTC instead of CD; (4) `predict_proba` columns were passed to `roc_auc_score` in `model.classes_` order without reordering, corrupting ~32 AUCs. All are fixed in the working tree; the pre-fix state is preserved at tag `pre-leadfix`. Correcting the lead order alone lifts cross-domain territory transfer from chance (p≈0.76) to p≈0.002 — **but C2ST still sits at 1.0**, so the alignment dead-end below survives the fix. ⚠ **That transfer movement is corroboration, not proof** — the Stage 4.2 sweep (bullet below) shows 39/66 arbitrary transpositions also improve transfer. The fix is carried by the lead-identity check `aVL = (I − III)/2` verified on the signals; cite that, not the p-value. Full record: `reports/2026-08-10_repo_audit_and_rerun_plan.md`, `reports/EXECUTION_LOG_2026-08-10.md`.
- **Ruled out** — direct latent-space alignment between synthetic MedalCare-XL and real PTB-XL is a confirmed dead-end. Single-bandwidth MMD, multi-bandwidth MMD, class-conditional MMD, and INLP each reduce first-order distance metrics, but C2ST AUROC stays ~1.0 on held-out latents. Do not propose new alignment-via-training approaches without acknowledging this. Full record: `reports/inlp_alignment_summary.md`, `reports/exp7_progress_report.md`.
- **Currently pushing** — biophysical θ-decoding from latents (Phase B / B2). In-domain MedalCare decoding of `isch[0].{phi, z, size, rho_eps_max}` and APD parameters works; cross-domain transfer to PTB-XL is the open problem. The 2026-05-13 Track 3 redux (refined 4-class anatomical territory) is the active diagnostic — see `reports/b2cd_redux_log.md` and `analysis/phase_b2_infarct_decoding.py`.
- **⚠ 2026-08-12 — the pre-registered Track 3 decision rule is now SCORED, and the verdict hinges on an unresolved scaler choice.** Rule (fixed long before the numbers): POSITIVE = 4-class macro-F1 ≥ 0.45, p<0.01; PARTIAL = 2-class ≥ 0.65, p<0.01; else NEGATIVE. On the clean `exp8_leadfix_medalonly` encoder at n=4324: **4-class 0.3440 (`target`) / 0.3357 (strict) — POSITIVE not met under either.** **2-class 0.6299 (`target`) / 0.6521 (strict) — PARTIAL literally met under the strict scaler, missed under the legacy one.** On that same strict/2-class cell the latent also beats the 54-feature control (Δ=+0.0195, paired p=0.044) — the only cross-domain cell in twelve where it does. **Write "PARTIAL met under the pre-specified strict scaler; not met under the legacy one", lead with the 4-class primary endpoint, and do NOT flatten this to either a clean NEGATIVE or a clean PARTIAL.** Which scaler is primary is a supervisor call (`reports/2026-08-11_integrity_audit_and_probe_map.md` §5 item 5): §1.5 argued strict is the principled mode *before* these numbers existed and while the concern ran the other way, but it post-dates pre-registration, the margin is 0.0021, and the arm-vs-arm sign flips between scalers. **⚠ At n=4324 every `permutation_p_macro_f1` sits at the 1e-4 floor for BOTH arms on BOTH endpoints** — "beats chance" is uninformative at this power (trivially true of the hand-crafted control too), so never quote a floor p-value as evidence the latent works; only the paired arm-vs-arm test discriminates. Full paired grid: §6.4c; regenerate with `python scripts/_audit_paired_grid.py`.
- **⚠ 2026-08-12 — SUPERSEDES the fair-control result. The cross-domain endpoint is a NULL, not a control win.** The 2026-08-11 claim that a 54-feature hand-crafted spatial control *beats* the 1024-d latent cross-domain was **conditional on the encoder having itself been supervised on PTB-XL**, and does not survive removing that. Training an otherwise byte-identical MedalCare-only encoder (`--medalcare-only`, no PTB-XL gradient step, checkpoint selected on MedalCare val F1 alone) frees all 10 PTB-XL folds for evaluation (n=438 → **4324**, 9.9×). Primary endpoint, paired swap test, Δ = latent − control:

  | encoder | eval rows | `target` scaler | strict scaler |
  |---|---|---|---|
  | baseline (saw PTB-XL) | fold 10, n=438 | −0.0656, **p=0.040** | −0.0507, p=0.098 |
  | medalonly | fold 10, n=438 | −0.0331, p=0.286 | −0.0111, p=0.719 |
  | medalonly | **all folds, n=4324** | **+0.0145, p=0.162** | **+0.0041, p=0.695** |

  **Write "statistically indistinguishable", NOT "the latent wins"** — the sign flips but nothing is detectable at any power. Encoder-independence is proven, not assumed: the control reproduces to four decimals (0.3442 / 0.3304) in every fold-10 cell, so only the latent moved. The encoder contributes +0.033/+0.040 consistently across scalers; the eval-set contribution (+0.048/+0.015) is inconsistent and lies inside the n=438 CI, i.e. sampling noise. Imputation confound excluded in the control's favour (all-54-finite 75.3% all-folds vs 72.4% fold-10). ⚠ **This null is the 4-class *primary* endpoint.** On the 2-class *secondary* endpoint under the strict scaler the latent does beat the control (0.6521 vs 0.6326, Δ=+0.0195, p=0.044, CI [−0.0000,+0.0390]) — one nominal hit in **twelve** paired cross-domain tests, matched by one in the opposite direction (baseline/`target`/fold-10, p=0.040); neither survives Holm across the twelve. Report both; do not quote either alone. ⚠ Note also that the published control-win was **never robust to the scaler even on its own encoder**: the same baseline/fold-10 cell gives p=0.040 under `target` but p=0.098 under strict — one significant cell out of the four (encoder × scaler) eventually run at n=438. Full record: `reports/2026-08-11_integrity_audit_and_probe_map.md` §6.4b (verdict), §6.4c (full twelve-row paired grid).
- **2026-08-11 — the surviving half of the fair-control result: the latent's advantage is real, large, and in-domain only.** In-domain θ-territory decoding scores identical MedalCare rows (n=1200) in every run, so it is a controlled encoder comparison: latent beats the 54-feature control by **Δ=+0.1523 macro-F1, p=0.0001** (0.6560 vs 0.5037) — up from +0.1367 on the PTB-XL-supervised encoders. Cross-domain that entire surplus vanishes (bullet above). So the latent's extra θ-territory information is real and large, and is **exactly the part that is synthetic-specific**. Not a dimensionality artifact — `C` is tuned per arm on source CV, and the 64-d K64 latent transfers *worse* than the 54-d control. A candidate mechanism is measured at the channel level: MedalCare encodes territory in ST (acute injury current), PTB-XL in Q/R (chronic infarct) — see memory `discovery_st_channel_divergence`, and `reports/2026-08-11_integrity_audit_and_probe_map.md` §2.6 for the clinical grounding (Thygesen et al., *Eur Heart J* 2018;40(3):237–269, DOI 10.1093/eurheartj/ehy462, for the acute-ST vs prior-MI-Q criteria; Das et al., *Circulation* 2006;113(21):2495–2501, DOI 10.1161/circulationaha.105.595892, for Q-wave regression over time — both `papersflow`-verified, do not re-key from memory). ⚠ **2026-08-13 — call it "consistent with", not "the mechanism for".** PTB-XL *does* carry acuity (`infarction_stadium1`, 1617/4324 graded, **90.2% Stadium II-III/III** — chronic dominance is now measured, not inferred), but the readout does **not** transfer better onto acute rows: territory-centred alignment vs acuity rank gives ρ=−0.034 (p=0.097) under the strict scaler and ρ=**+0.017** (p=0.743, wrong sign) under `target`, non-monotone in both — the apparent effect is entirely the n=61 Stadium-II stratum, at the wrong end of the axis. (A per-stratum "b̂" analysis previously quoted here is **retracted 2026-08-13**: the S(b) statistic it used is well-defined only at integer b — every non-integer b̂ is an artifact of the anchor-unwrap origin; see the banner in `analysis/cyclic_order_test.py`. The surviving statistic is the `acuity_trend()` rank test just quoted, which does not use S(b).) `analysis/acuity_stratified_transport.py`. Pre-registered before the numbers existed: `reports/EXECUTION_LOG_2026-08-10.md` Part 16 (rules) → Part 18 (verdict).
- **⚠ Every cross-domain arm-vs-arm claim needs `paired_Z_vs_features`, not two marginal CIs.** Until 2026-08-11 every number was tested against its *own* label-shuffle null ("beats chance?"), never arm-against-arm — which is the claim actually being made. `paired_macro_f1()` in `analysis/phase_b2_infarct_decoding.py` scores both arms on the same resampled rows (bootstrap CI on Δ) and swaps their predictions per row (paired permutation). It draws its own `derive_rng(cfg, "paired_Z_vs_features", endpoint)` stream, so it shifts no downstream p-value. Overlapping marginal CIs do **not** settle an ordering.
- **2026-08-11 — C2ST is constitutionally blind; transfer only detects gross corruption.** A 77-cell lead-permutation sweep (identity + all 66 transpositions + 10 random permutations, applied to PTB-XL test at inference with the probe fit once on MedalCare-train and never refit; identity reproduces the stored export to `max|d|=0`). Across cells spanning **2× in transfer** (macro-F1 0.162–0.320), linear C2ST spread is **1e-5** and GBDT C2ST **9e-5**, neither correlating with damage (ρ = −0.04, −0.03) — a statistic saturated at 1.0 cannot tell a physiological gap from a channel transposition. MMD² does move (72% of mean) and separates random permutations from transpositions (p=3.9e-07), but does not track harm within transpositions (ρ=−0.146, p=0.20): it flags *change*, not *damage*. **Transfer detects gross corruption** (randoms: 7/10 below identity's CI, 0/10 significant) **but not single transpositions** (0/66 below identity's CI; 39/66 score *above* correct order; the historical aVL↔aVF cell ranks 70/77 in damage). Caveat: identity itself is only p=0.1139 at n=438, so the null is partly a power statement. **Do not claim transfer is a lead-order detector — it is a corruption detector.** `reports/EXECUTION_LOG_2026-08-10.md` Part 19, `reports/2026-08-11_breakthrough_analysis.md` §17, `outputs/analysis/leadperm_sweep/`.

**Working mode**: collaborative research, not code-execution-on-demand. When results are weak, default to discussing honest framings — negative-result writeups, methodology critiques, pivot proposals — *before* running the next experiment. The `reports/` directory is the source of truth for what's been tried. See memory `project_research_state.md` (paper-claim structure, phase status) and `project_methodology_decisions.md` (ruled-out approaches and why).

> **Note for fresh clones**: `reports/` is gitignored — the supervisor-facing writeups cited above (`inlp_alignment_summary.md`, `exp7_progress_report.md`, `b2cd_redux_log.md`) are owner-local only. The headline findings are summarised here; if you don't have `reports/`, treat this file plus the rule files as the authoritative summary.

For tech stack, label spaces, and external dependencies see [`openspec/project.md`](openspec/project.md) — canonical project-context file (now committed; was previously gitignored).

## Project Structure

- `scripts/`, `analysis/` — CLI scripts (data prep, training, latent export, audits) and post-training analysis (alignment, dim-scan, B2 decoding, INLP).
- `data/`, `outputs/`, `checkpoint/`, `reports/` — seeded manifests + θ NPZs (do not regenerate); per-run experiment artifacts (gitignored); pre-trained ECGFounder weights; supervisor-facing writeups.
- `losses/`, `metrics/` — MMD variants; multilabel metric suite (F1, AUC, Brier, recall, precision, specificity).
- `MedalCare-XL/`, `ptb-xl-...` — raw synthetic and real datasets on disk (gitignored); θ targets parsed from `MedalCare-XL/WP2_largeDataset_ParameterFiles/*VentricularParameters.txt`.
- `openspec/` — spec-driven development: `specs/`, `changes/`, `AGENTS.md`.
- `net1d.py`, `finetune_model.py`, `util.py` — root-level: backbone + adapter, model builders, eval/checkpoint helpers.

## Tech Stack

Python 3.10.19, PyTorch 2.9.1+cu128 (NVIDIA CUDA 12.8), wfdb 4.2.0, neurokit2 0.2.10 (pip-only — not in conda env), scikit-learn 1.6.1. No PyTorch Lightning, no Transformers, no `pyproject.toml`. Canonical install: `env-ECGFounder.yml`; pip subset: `requirements.txt`.

## Conventions (load-bearing one-liners; full detail in `.claude/rules/`)

- **Lead order** — reindex by WFDB `sig_name`, never by position, on **both** domains. MedalCare WFDB is already in standard order `[…, aVR, aVL, aVF, V1…]`; the historical positional aVF↔aVL swap was a **bug**, fixed 2026-08-10. See `data-pipeline.md`.
- **Per-lead z-score on both domains** — MedalCare used to use a single global scalar; that was a second synthetic-vs-real mismatch. See `data-pipeline.md`.
- **`sys.path` injection** — every entry-point script under `scripts/` and `analysis/` prepends `REPO_ROOT` to `sys.path` before importing root modules. See `model-conventions.md`.
- **`outputs/<run_id>/` artifact contract** — every fine-tune emits `args.json`, `metrics.json`, `per_class_metrics.csv`, and `checkpoints/linear_best.pt` (⚠ **not** `best_model.pt` — that name was wrong in these docs until 2026-08-12). See `experiments.md`.
- **`linear_prob=True + use_adapter=True` is BUGGED** in `ft_12lead_ECGFounder` — freezes `model.dense`. Use the manual-freeze workaround. See `model-conventions.md`.
- **MedalCare label_0..label_7 order is FIXED** as `(sinus, mi, rbbb, lbbb, lae, iab, fam, avblock)`; `MEDALCARE_REMAP` indexes by integer position. See `data-pipeline.md`.
- **θ is 4-dimensional** — `phi, z, size, rho_eps_max`; `transmural` is a duplicate array, not a fifth parameter. `territory_4c` is derived from φ. See `data-pipeline.md`.

## Detailed rules

| File | When to read |
|---|---|
| [`.claude/rules/commands.md`](.claude/rules/commands.md) | About to run any CLI (install, data prep, train, export, analyze). |
| [`.claude/rules/experiments.md`](.claude/rules/experiments.md) | Interpreting `outputs/<run_id>/` artifacts; designing or comparing Exp 5/6/7 runs. |
| [`.claude/rules/data-pipeline.md`](.claude/rules/data-pipeline.md) | Editing Dataset classes, manifest builders, or `scripts/build_*.py`. |
| [`.claude/rules/model-conventions.md`](.claude/rules/model-conventions.md) | Editing `net1d.py`, `finetune_model.py`, or writing a new training entry-point. |

OpenSpec workflow: see [`openspec/AGENTS.md`](openspec/AGENTS.md). **Do not write code during the proposal stage.** Skip proposals for bug fixes, typos, config tweaks, and tests for existing behaviour.

## MCP Tool Usage

Three MCP servers are wired into this project: **context7** (current library/framework docs), **papersflow** (academic literature + citation verification), and **brave-search** (general web).

### When writing the thesis / paper
- Before citing any paper: `papersflow.verify_citation` to normalize the reference. Do NOT cite from training-data memory — DOIs and venue attributions drift (e.g., Li et al. was previously miscited as "MICCAI 2024" when the correct venue is IEEE TMI 2024).
- Searching literature on a topic: use `papersflow.search_literature` (peer-reviewed academic record) AND `brave-search.brave_web_search` (adjacent non-peer-reviewed material — preprints not yet indexed, code releases, researcher blogs, workshop pages, GitHub repos, social-media discussion of papers) in tandem. The two are complementary. Then expand the academic cluster via `papersflow.find_related_papers` and `papersflow.get_citation_graph`.
- Mapping prior work around a known paper (references + citing papers): `papersflow.get_citation_graph` / `expand_citation_graph`.

### When coding
- API, version, or migration questions on `torch`, `scikit-learn`, `neurokit2`, `wfdb`, etc.: `context7.resolve-library-id` → `context7.query-docs`. Use even for libraries you "know" — training data may be stale. Do NOT use WebSearch — it returns out-of-date tutorial blogs.
- Debugging *project* business logic, refactoring, or anything in this repo's source: use `Read` / `Grep` / `Glob`. Context7 is for upstream library docs, not local code.

### When researching outside academia
- Vendor blogs, product pages, news, current events: `brave-search.brave_web_search`.
- Location / business queries: `brave-search.brave_local_search`.
- Do NOT use brave-search for: library docs (→ context7), citation verification (→ `papersflow.verify_citation`). For literature *discovery*, brave-search is a legitimate complement to papersflow.

### Anti-patterns
- Never fabricate a citation, DOI, or venue — verify via `papersflow.verify_citation` first.
- Never trust your memory of a library API for code that will actually run — query `context7` and quote the result.
