# Forbidden phrasings — derived from reports/2026-08-13_thesis_endgame_decision.txt Section 4 (items 1–26).
# Usage:  grep -n -i -E -f notes/forbidden_phrases.txt chapters/*.tex
# One extended regex per line; lines starting with # are comments (strip them: grep -v '^#' > /tmp/fp; grep -f /tmp/fp).
# A hit is not automatically wrong — it is a mandatory review point. Add the item number in the commit message when a hit is cleared.
# 1 / A.1
b[_ -]?hat
hat\{b\}
per-stratum (slope|b)
the mechanism for
# 2
unit (angular )?gain
# 3
five of (the )?six encoders
six encoders
# 4
latent (beats|outperforms|exceeds) the axis
beats the (frontal )?axis
# 5
p ?[=<] ?1e-4
p ?= ?0\.001 as evidence
beats chance
# 6
latent (wins|beats the control)
control(54)? beats the latent
# 7 (rewritten 2026-08-18: dated bar is 0.55; verdict met under BOTH modes; never flatten)
clean(ly)? (negative|partial)
(≥|>=) ?0\.65.{0,40}(pre-?registered|pre-?specified)
(hinges|turns) (entirely )?on (the|which) scaler
# 8
recovered better in real
# 9
p ?= ?0\.76
lead-order detector
# 11
significantly anti-?correlated
feature ranking is significantly
# 12
marginal realism is anti-?correlated
anti-?correlated with the informativeness gap
# 13
rho ?= ?-0\.9(00)?[^,;]*p ?= ?0\.03[7]
# 15
all 13 Q/R
29 blind spots
8 spurious
# 16
signs agree for every
# 18
0\.936
# 19
repair achieved
inferior[- ]lead(s)? (win|beat)
# 20
significantly worse than random
# 24
0\.178[^0-9].{0,40}zero-shot
# 25
only (validated|checked) marginals
only validated marginal
# 26 / general
first sim2real
first sim-to-real ECG
first synthetic-to-real ECG
# hygiene (review-only hits): every \circR / "R =" number must be followed by its floor within the sentence
circR ?(=|of|is) ?0\.[0-9]{2,4}
R̄ ?= ?0\.[0-9]{2,4}
# NAMING TRAP (owner ruling 2026-08-18): 'strict' must never appear unqualified — name the mode.
# Allowed forms: strict (`target_pool_measured`), strict transport (`source`), strict/source, strict = ..., 	exttt{...}
strict(?!\s*(\(|=|/|transport|-transport|mode|scaler\s*\(|\texttt|`))
